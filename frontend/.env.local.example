# URL base da API versionada
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# URL pública do site — usada para montar URLs absolutas de /media/ para o Next.js <Image>
# Em dev: mesmo host do backend (http://localhost:8000)
# Em prod: domínio público do site (https://virtu.com.br)
NEXT_PUBLIC_SITE_URL=http://localhost:8000
